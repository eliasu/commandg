
name: 🚀 Deploy Website to All-Inkl

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Jeah
        uses: mdallasanta/ssh-scp-deploy@v1.2.0
        with:
          local: './'                                                  # Local file path - REQUIRED false - DEFAULT ./
          remote: '~/www/htdocs/w01abbc7/dev.command-g.de/'                                                 # Remote file path - REQUIRED false - DEFAULT ~/
          host: w01abbc7.kasserver.com                                      # Remote server address - REQUIRED true
          user: ssh-w01abbc7
          password: ${{secrets.FTPPASS}}                              # User password - REQUIRED at least one of "password" or "key" 
          key: ${{secrets.SSH_KEY}}                                        # Remote server private key - REQUIRED at least one of "password" or "key" 
          pre_upload: echo "This will be executed before the upload!"  # Command to run via ssh before scp upload - REQUIRED false
          post_upload: echo "This will be executed after the upload!"  # Command to run via ssh after scp upload - REQUIRED false
          ssh_options: -o StrictHostKeyChecking=no                     # A set of ssh_option separated by -o - REQUIRED false - DEFAULT -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null
          scp_options: -v                                              # Flags to use during scp - REQUIRED false - DEFAULT ''